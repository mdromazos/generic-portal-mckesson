import React from 'react';

// translations for the parent app
import {mdmLayoutTranslations} from '../i18n';

// components
import layout from './layout'


export const Layout = {
    mdmLayoutTranslations: Object.assign({}, mdmLayoutTranslations),
    Layout: layout
};
